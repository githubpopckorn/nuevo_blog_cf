 	<nav class="navbar navbar-inverse navbar-static-top hidde_button_margin">
		<div class="container">

			<%= link_to "Blog Facilito", root_path, class: "navbar-brand"%>
            <ul class="nav navbar-nav">
               <li><%= link_to "Articulos", articles_path %> </li>
               <% @categories.each do |category|  %>
                <li>
                    <%= link_to category.name, category %>
                </li>
            <% end %>
               
            </ul>
            <% if user_signed_in? %>
              <ul class="nav navbar-nav navbar-right">
                <li><%= link_to "Cerrar sesión", destroy_user_session_path, method: :delete %></li>
              </ul>
            <% else %>
                <ul class="nav navbar-nav navbar-right">
                    <li><%= link_to "Iniciar sesión", new_user_session_path %></li>
                    <li><%= link_to "Crear cuenta", new_user_registration_path %></li>
                </ul>
            <% end %>
            
		</div>
	</nav>